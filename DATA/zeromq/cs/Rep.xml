NetMQ.Core.Patterns JetBrains.Annotations using class NetMQ.Core.Patterns.Rep super super Router bool bool bool bool protected override protected override ref ref if ! throw new decl_stmt bool = decl_stmt bool = ref if ! return if ! = return bool bool protected override protected override ref ref decl_stmt bool if throw new if while = ref if ! return if decl_stmt bool = == = ref if ! return if break else = = ref if ! return if ! = = return protected override protected override if return return protected override protected override if ! return return