Spring System using System.Diagnostics using System.Reflection using NUnit.Framework using class Spring.CompilerOptionsTests Assembly public public decl object private private decl_stmt object = typeof if && if == endif if && ! if == else endif DebuggableAttribute private private foreach in if is decl_stmt DebuggableAttribute = as if if DebuggableAttribute private private foreach in if is decl_stmt DebuggableAttribute = as if == if == return =